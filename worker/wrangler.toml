name = "surge-ip-labeler"
main = "src/index.js"
compatibility_date = "2026-07-14"

# Configure SYNC_TOKEN and READ_TOKEN as Worker secrets; never add their values here.
[[kv_namespaces]]
binding = "POLICIES"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
